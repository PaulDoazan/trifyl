export const STAGE_WIDTH = 1920;
export const STAGE_HEIGHT = 1080;
export const MENU_WIDTH = 528;

// Zone de grille telle que dessinée (cadre pointillé) dans les images grille_nivX.png,
// mesurée sur les assets — identique pour les 3 niveaux. La grille de jeu se cale dessus.
export const GRID_RECT = { x: 693, y: 52, width: 996, height: 977 } as const;
